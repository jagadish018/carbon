generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//student table
model Student {
  id                String              @id @default(uuid())
  name              String
  dateOfBirth       String
  aadharNumber      String              @unique

}
